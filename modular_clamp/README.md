# Modular CLAMP
This is the root directory for the CLAMP modular components.

## Structure
- `code/` - For code files that don't fit into modules
- `modules/` - For modular features
- `icons/` - For icon files
- `sound/` - For audio files
